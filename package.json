{
  "name": "o2-sensor-inventory",
  "version": "1.0.0",
  "description": "Garage O2 sensor inventory tracker with a shared Postgres backend, deployable on Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
